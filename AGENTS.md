# donvalue-bot 작업 규칙

이 저장소를 고치는 모든 에이전트(Codex/GPT·Claude)가 지킬 것. 사람이 매번 중재하지 않아도
되도록, 아래는 테스트로도 막혀 있다.

## 0. 푸시 전에 반드시

```
python -m unittest discover -s codex/tests -q
```

**OK 가 아니면 푸시하지 않는다.** 이 테스트는 `codex-approval` 워크플로가 약 5분 간격으로 돌리는
것과 같다. 실패한 채로 푸시하면 워크플로가 계속 실패하면서 **미리보기와 발행이 통째로 멈춘다.**
실제로 두 번 멈췄다.

| 날짜 | 원인 | 결과 |
|---|---|---|
| 2026-09-14 22:26 | 회귀 테스트를 추가하면서 정규식 오타 + 기존 금액 규칙과 충돌 | 약 20시간 정지 |
| 2026-09-15 19:02 | 이미 찬 슬롯(9/16 20:00)에 편을 하나 더 넣음 | 즉시 정지 |

## 1. 두 트랙은 서로 건드리지 않는다

| 트랙 | 코드 | 콘텐츠 | 발행 요일(20:00 KST) |
|---|---|---|---|
| GPT / Codex | `codex/` | `codex/content.json` | 월·수·금·토 |
| Claude | `src/` | `content/queue.yaml` | 화·목·일 |

공용은 `.github/workflows/`, `content/telegram_owner.enc`, 텔레그램 봇,
`codex/approvals.py` 및 승인 수신기 `codex/engine.py`다.
2026-09-19 소유자 요청으로 두 트랙의 승인 처리를 통합했다.
상대 트랙 파일을 고쳐야 하면 먼저 사람에게 묻는다.

## 2. 큐에 편을 넣거나 뺄 때

1. **한 슬롯에 한 편.** `publish_at` 중복 금지. 빈 슬롯을 먼저 찾고, 없으면 마지막 편 뒤에 붙인다.
2. **`codex/tests/test_seed_three.py` 의 편수도 같이 고친다.** 편이 실수로 사라지는 걸 잡으려고
   하드코딩해 둔 값이다.
3. **`hook` 을 비워 두지 않는다.** 비면 표지 문구가 빈 채로 렌더된다. 보통 `slides[0]['title']`.
4. 가격 출처가 없는 편(`verification.real_world_price_claim: false`)이 캡션에 금액을 쓰려면
   ① 캡션에 가정임을 명시 ② 쓰레드 본문에도 명시 ③ 조건이 뒤집히는 지점(손익분기)을 함께 적는다.

## 3. 두 트랙 모두 같은 승인 / 미루기 흐름을 쓴다

발행 전날 20:00에 미리보기와 **[승인] / [미루기]** 버튼을 보낸다.
승인은 콘텐츠·미디어 버전에 묶어 저장하고, 저장 성공 뒤 **승인됐어요**와 게시 예정 시간을 보낸다.
미루기를 누르면 다시 승인하기 전에는 게시하지 않는다. 원래 예정 시간이 지난 뒤 승인하면
다음 실행에서 게시할 수 있도록 새 예정 시간을 저장한다. 실행 지연이 있을 수 있다.

- 텔레그램 `getUpdates` 수신은 **`codex.engine.Engine.callbacks` 한 곳만** 담당한다.
  다른 작업에서 조회하거나 offset을 비우면 상대 트랙의 승인이 사라진다. 추가하지 않는다.
- GPT: `codex/state.enc`의 `records`에 승인과 게시 상태를 저장한다.
- Claude: `eve-approval.yml`은 미리보기와 `content/reviews/*.enc` 요청만 등록하고 종료한다.
  공용 수신기가 `codex/state.enc`의 `shared_reviews`에 결정을 저장하고 기존 발행기를 호출한다.
- `codex-approval.yml`은 약 5분 간격으로 수신·확인 메시지·예정된 게시를 처리한다.
- 승인한 결과물은 다시 렌더하지 않고 그대로 쓴다. 내용이나 파일이 달라지면 새 승인이 필요하다.
- 일반 게시에는 승인이 필요하다. 과거 누락분의 별도 복구는 소유자가 명시적으로 허락한
  대상·콘텐츠 버전·기간에 한하며, 실제 피드와 게시 기록을 먼저 확인해 중복을 막는다.

## 4. 이미 발행된 편은 건드리지 않는다

`content/posted.json`, `content/delivery.json`, `codex/state.enc` 에 기록이 있는 편은
내용을 바꾸지 않는다. SNS 에 이미 나간 것과 저장소가 어긋난다.

## 5. 내부 값과 공개 문구를 섞지 않는다

`track`(어느 트랙에서 왔는지)은 내부용이다. 사람에게 보여줄 출처는 `sources` 에만 넣는다.
예전에 이 둘을 한 필드로 쓰다가 유튜브 설명란에 "기준·출처: codex" 가 그대로 나갔다.

## 6. 유튜브 (2026-09-20 심사 통과 → 보류 해제)

지금 상태: `content/youtube_hold.json` 의 `hold` 는 **false**. 세 곳에 같은 날 올라간다.

| 경로 | 무엇을 올리나 | 언제 |
|---|---|---|
| `post.yml` → `src/run.py` | Claude 트랙 릴스 편의 mp4 를 쇼츠로 | 인스타·쓰레드와 같은 자리 (YT_AUTO=1, public) |
| `youtube-shorts.yml` → `src/youtube_run.py --auto` | 카드뉴스 편 · GPT 트랙 편 | 하루 종일 20분 간격, 하루 `YT_DAILY_MAX`(기본 3)편까지 |

- 같은 편을 두 번 올리지 않는 근거는 `content/delivery.json` 의 `youtube` 키 하나다.
  이 원장을 건너뛰고 업로드하는 코드를 새로 만들지 않는다.
- 하루 상한을 크론으로 대신하지 않는다. 20분 간격 × `--count 3` 이면 할당량
  (업로드 1건 1,600 units / 기본 10,000)을 오전에 다 태운다.
  회귀 테스트: `codex/tests/test_youtube_daily_cap.py`
- 올린 뒤 **실제 공개상태를 채널에서 다시 읽는다.** 요청(public)과 다르면 텔레그램으로
  알린다. 업로드 응답만 보고 완료로 치지 않는다.

### 6-1. 보류 스위치는 남겨 둔다 (심사가 다시 막힐 때)

구글 심사(audit) 전에 `videos.insert` 로 올린 영상은 **비공개로 잠기고 채널 주인이
손으로도 공개로 못 바꾼다.** 그런데 올라간 순간 `content/delivery.json` 에
`youtube: done` 으로 남아, 나중에 심사가 풀려 몰아 올릴 때 그 편들을 전부 건너뛴다.
= 영영 공개 못 하는 편이 조용히 쌓인다.

그래서 `content/youtube_hold.json` 의 `hold: true` 동안 **정기 발행(`src/run.py`)은
유튜브만 건너뛴다.** 인스타·쓰레드는 그대로 나간다.

- 이 검사를 지우거나 우회하지 않는다. 회귀 테스트: `codex/tests/test_youtube_hold.py`
- 설정 파일이 없거나 깨졌으면 **보류**가 기본이다. 기본값을 '올림'으로 바꾸지 않는다.
- 손으로 돌리는 백필(`--id` / `--count`, 워크플로 수동 실행)은 이 값과 하루 상한
  둘 다와 무관하게 올라간다. 밀린 편을 몰아 올리는 경로가 그것이다.
- 막는 방법: `hold` 를 `true` 로 바꾼다. 정기 발행과 정기 따라올리기가 즉시 멈춘다.
  푸는 방법도 그 반대 하나뿐이다.
